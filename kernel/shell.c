#include "system.h"
void shell(char* cmd)
{
   /* print(cmd);
    if(strcmp(cmd,"help")==0)
        help();
    else if(strcmp(cmd,"clear")==0)
        clear_screen();
    else
        print("Invalid Command\n");
	*/
}